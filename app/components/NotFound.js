import React, { PropTypes } from 'react';

const NotFound = () =>  (
    <div className="">
        NOT FOUND
    </div>
);

NotFound.propTypes = {
    str: PropTypes.string
};

export default NotFound;
